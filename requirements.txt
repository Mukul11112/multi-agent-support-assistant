# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic[email]==2.9.2

# --- Auth ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
# NOTE: passlib is deliberately NOT used. passlib 1.7.4 is incompatible with
# bcrypt >= 4.1 and raises "password cannot be longer than 72 bytes" on every
# call. backend/api/auth.py calls bcrypt directly instead.

# --- RAG ---
sentence-transformers==3.0.1
faiss-cpu==1.8.0
pypdf==5.0.1
numpy==1.26.4
scikit-learn==1.5.1        # offline fallback embedder only

# --- Database ---
pymongo==4.8.0

# --- LLM providers (install only the one you use) ---
groq==0.11.0
# openai==1.51.0
# google-generativeai==0.8.3
requests==2.32.3

# --- Utilities ---
python-dotenv==1.0.1
reportlab==4.2.2           # builds the knowledge base PDFs

# --- Tests ---
pytest==8.3.3
httpx==0.27.2
